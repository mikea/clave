[package]
name = "clave"
version = "0.3.0"
edition = "2021"
description = "Clave is midi click-track generator"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
midly = { version = "0" }
regex = { version = "1" }